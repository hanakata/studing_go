第13章 低レベルプログラミング
■unsafe.Sizeof、Alignof、Offsetof
unsafe.Sizeof関数はそのオペランド表現のバイト数での大きさを報告する。
Alignof関数はその引数の型に必要な整列を報告する。
Offsetof関数は穴があればそれも考慮して構造体xの先頭から相対的なフィールドfのオフセットを計算する。

unsafe.Pointer型はいかなる変数のアドレスでも保持できる特殊な種類のポインタ。
unsafe.Pointerを通した参照は出来ない。

■cgoを使ったCのコードの呼び出し
C関数に対するGoでのバインディングを行うツール＝cgo